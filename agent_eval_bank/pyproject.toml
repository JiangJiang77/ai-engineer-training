[project]
name = "agent-eval-bank"
version = "0.1.0"
description = "Offline question bank for evaluating AI agent application engineering skills."
requires-python = ">=3.9"
dependencies = [
  "pytest",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
